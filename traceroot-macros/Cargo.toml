[package]
name = "traceroot-macros"   # ✅ must match in both places
version = "0.1.0"
edition = "2021"

[lib]
proc-macro = true

[dependencies]
quote = "1.0"
syn = { version = "1.0.109", features = ["full"] }
proc-macro2 = "1.0"
